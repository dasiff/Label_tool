[pytest]
# Prevent some third-party pytest plugins (e.g. napari) from auto-loading during test discovery
# This avoids unrelated import-time errors in developer environments that lack optional deps.
addopts = -p no:napari
